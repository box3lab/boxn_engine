import { Vector3 } from "@babylonjs/core";

/**
 * 常量类 / Constant class
 * 存储一些常用的常量值
 */
export class Const {
    /**
     * 重力常量 / Gravity constant
     */
    static readonly GRAVITY = new Vector3(0, -9.81, 0);
}
